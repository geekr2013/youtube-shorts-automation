name: Daily YouTube Shorts Upload

on:
  schedule:
    - cron: '0 0 * * *'  # 매일 UTC 0시 = 한국시간 오전 9시
  workflow_dispatch:  # 수동 실행 가능

permissions:
  contents: write  # 히스토리 파일 자동 커밋 권한

jobs:
  upload:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Cache pip packages
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      
      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
      
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Load history files
        run: |
          if [ -f downloaded_history.txt ]; then
            echo "✅ 영상 히스토리 로드됨"
          else
            touch downloaded_history.txt
            echo "📝 새 영상 히스토리 파일 생성"
          fi
          if [ -f music_history.txt ]; then
            echo "✅ 음악 히스토리 로드됨"
          else
            touch music_history.txt
            echo "📝 새 음악 히스토리 파일 생성"
          fi
      
      - name: Run automation
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          RECEIVER_EMAIL: ${{ secrets.RECEIVER_EMAIL }}
          GMAIL_PASSWORD: ${{ secrets.GMAIL_PASSWORD }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
        run: |
          python src/main.py
      
      - name: Save history files
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add downloaded_history.txt music_history.txt
          git diff --staged --quiet || git commit -m "Update download history [skip ci]"
          git push
      
      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: execution-logs
          path: '*.log'
